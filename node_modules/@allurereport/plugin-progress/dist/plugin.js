var __classPrivateFieldSet = (this && this.__classPrivateFieldSet) || function (receiver, state, value, kind, f) {
    if (kind === "m") throw new TypeError("Private method is not writable");
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a setter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
    return (kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value)), value;
};
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _ProgressPlugin_terminal, _ProgressPlugin_preserve, _ProgressPlugin_minRenderIntervalMs, _ProgressPlugin_lastRenderAt, _ProgressPlugin_pendingStore, _ProgressPlugin_pumping, _ProgressPlugin_pumpDone, _ProgressPlugin_pump, _ProgressPlugin_render;
import console from "node:console";
import * as process from "node:process";
import { setTimeout as delay } from "node:timers/promises";
import { green, red, yellow } from "yoctocolors";
import { Terminal } from "./terminal.js";
const DEFAULT_MIN_RENDER_INTERVAL_MS = 300;
export class ProgressPlugin {
    constructor(options = {}) {
        this.options = options;
        _ProgressPlugin_terminal.set(this, void 0);
        _ProgressPlugin_preserve.set(this, void 0);
        _ProgressPlugin_minRenderIntervalMs.set(this, void 0);
        _ProgressPlugin_lastRenderAt.set(this, 0);
        _ProgressPlugin_pendingStore.set(this, void 0);
        _ProgressPlugin_pumping.set(this, false);
        _ProgressPlugin_pumpDone.set(this, Promise.resolve());
        this.start = async (context, store, realtime) => {
            realtime.onTestResults(async () => {
                __classPrivateFieldSet(this, _ProgressPlugin_pendingStore, store, "f");
                if (!__classPrivateFieldGet(this, _ProgressPlugin_pumping, "f")) {
                    __classPrivateFieldSet(this, _ProgressPlugin_pumpDone, __classPrivateFieldGet(this, _ProgressPlugin_pump, "f").call(this), "f");
                }
            });
        };
        this.done = async (context, store) => {
            try {
                await __classPrivateFieldGet(this, _ProgressPlugin_pumpDone, "f");
                await __classPrivateFieldGet(this, _ProgressPlugin_render, "f").call(this, store);
            }
            finally {
                if (__classPrivateFieldGet(this, _ProgressPlugin_terminal, "f")?.isTTY()) {
                    __classPrivateFieldGet(this, _ProgressPlugin_terminal, "f").newline();
                }
                __classPrivateFieldGet(this, _ProgressPlugin_terminal, "f")?.detach();
            }
        };
        _ProgressPlugin_pump.set(this, async () => {
            __classPrivateFieldSet(this, _ProgressPlugin_pumping, true, "f");
            try {
                while (__classPrivateFieldGet(this, _ProgressPlugin_pendingStore, "f")) {
                    const elapsed = Date.now() - __classPrivateFieldGet(this, _ProgressPlugin_lastRenderAt, "f");
                    const wait = Math.max(0, __classPrivateFieldGet(this, _ProgressPlugin_minRenderIntervalMs, "f") - elapsed);
                    if (wait > 0) {
                        await delay(wait);
                    }
                    const store = __classPrivateFieldGet(this, _ProgressPlugin_pendingStore, "f");
                    __classPrivateFieldSet(this, _ProgressPlugin_pendingStore, undefined, "f");
                    __classPrivateFieldSet(this, _ProgressPlugin_lastRenderAt, Date.now(), "f");
                    try {
                        await __classPrivateFieldGet(this, _ProgressPlugin_render, "f").call(this, store);
                    }
                    catch (error) {
                        console.error("can't render progress", error);
                    }
                }
            }
            finally {
                __classPrivateFieldSet(this, _ProgressPlugin_pumping, false, "f");
            }
        });
        _ProgressPlugin_render.set(this, async (store) => {
            if (!__classPrivateFieldGet(this, _ProgressPlugin_terminal, "f")) {
                return;
            }
            const testsStatistic = await store.testsStatistic();
            const parts = [
                testsStatistic.failed ? `${red("failed")}: ${testsStatistic.failed}` : undefined,
                testsStatistic.broken ? `${yellow("broken")}: ${testsStatistic.broken}` : undefined,
                testsStatistic.passed ? `${green("passed")}: ${testsStatistic.passed}` : undefined,
                `total: ${testsStatistic.total}`,
            ].filter((part) => !!part);
            const line = parts.join("  ");
            if (!__classPrivateFieldGet(this, _ProgressPlugin_terminal, "f").isTTY() || __classPrivateFieldGet(this, _ProgressPlugin_preserve, "f")) {
                __classPrivateFieldGet(this, _ProgressPlugin_terminal, "f").write(line);
                __classPrivateFieldGet(this, _ProgressPlugin_terminal, "f").newline();
                return;
            }
            __classPrivateFieldGet(this, _ProgressPlugin_terminal, "f").cursorTo(0);
            __classPrivateFieldGet(this, _ProgressPlugin_terminal, "f").clearLine();
            __classPrivateFieldGet(this, _ProgressPlugin_terminal, "f").write(line);
        });
        const { stream = process.stdout, preserve = false, minRenderIntervalMs = DEFAULT_MIN_RENDER_INTERVAL_MS, } = this.options;
        if (stream) {
            __classPrivateFieldSet(this, _ProgressPlugin_terminal, new Terminal(stream), "f");
        }
        __classPrivateFieldSet(this, _ProgressPlugin_preserve, preserve, "f");
        __classPrivateFieldSet(this, _ProgressPlugin_minRenderIntervalMs, minRenderIntervalMs, "f");
    }
}
_ProgressPlugin_terminal = new WeakMap(), _ProgressPlugin_preserve = new WeakMap(), _ProgressPlugin_minRenderIntervalMs = new WeakMap(), _ProgressPlugin_lastRenderAt = new WeakMap(), _ProgressPlugin_pendingStore = new WeakMap(), _ProgressPlugin_pumping = new WeakMap(), _ProgressPlugin_pumpDone = new WeakMap(), _ProgressPlugin_pump = new WeakMap(), _ProgressPlugin_render = new WeakMap();
