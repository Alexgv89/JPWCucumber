var __classPrivateFieldSet = (this && this.__classPrivateFieldSet) || function (receiver, state, value, kind, f) {
    if (kind === "m") throw new TypeError("Private method is not writable");
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a setter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
    return (kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value)), value;
};
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _Terminal_instances, _Terminal_stream, _Terminal_originalWrite, _Terminal_rawStream, _Terminal_wrapLines, _Terminal_dy, _Terminal_lineActive, _Terminal_interceptForeignWrites;
import { clearLine, clearScreenDown, cursorTo, moveCursor } from "node:readline";
const patchedStreams = new WeakMap();
export class Terminal {
    constructor(outputStream) {
        _Terminal_instances.add(this);
        _Terminal_stream.set(this, void 0);
        _Terminal_originalWrite.set(this, void 0);
        _Terminal_rawStream.set(this, void 0);
        _Terminal_wrapLines.set(this, void 0);
        _Terminal_dy.set(this, void 0);
        _Terminal_lineActive.set(this, false);
        __classPrivateFieldSet(this, _Terminal_stream, outputStream, "f");
        __classPrivateFieldSet(this, _Terminal_originalWrite, patchedStreams.get(outputStream)?.originalWrite ?? outputStream.write.bind(outputStream), "f");
        __classPrivateFieldSet(this, _Terminal_rawStream, { write: __classPrivateFieldGet(this, _Terminal_originalWrite, "f") }, "f");
        __classPrivateFieldSet(this, _Terminal_wrapLines, true, "f");
        __classPrivateFieldSet(this, _Terminal_dy, 0, "f");
        if (outputStream.isTTY) {
            __classPrivateFieldGet(this, _Terminal_instances, "m", _Terminal_interceptForeignWrites).call(this);
        }
    }
    detach() {
        const state = patchedStreams.get(__classPrivateFieldGet(this, _Terminal_stream, "f"));
        if (!state) {
            return;
        }
        state.owners.delete(this);
        if (state.owners.size === 0) {
            patchedStreams.delete(__classPrivateFieldGet(this, _Terminal_stream, "f"));
            __classPrivateFieldGet(this, _Terminal_stream, "f").write = state.originalWrite;
        }
    }
    cursorSave() {
        if (!__classPrivateFieldGet(this, _Terminal_stream, "f").isTTY) {
            return;
        }
        __classPrivateFieldGet(this, _Terminal_originalWrite, "f").call(this, "\x1B7");
    }
    cursorRestore() {
        if (!__classPrivateFieldGet(this, _Terminal_stream, "f").isTTY) {
            return;
        }
        __classPrivateFieldGet(this, _Terminal_originalWrite, "f").call(this, "\x1B8");
    }
    cursor(enabled) {
        if (!__classPrivateFieldGet(this, _Terminal_stream, "f").isTTY) {
            return;
        }
        if (enabled) {
            __classPrivateFieldGet(this, _Terminal_originalWrite, "f").call(this, "\x1B[?25h");
        }
        else {
            __classPrivateFieldGet(this, _Terminal_originalWrite, "f").call(this, "\x1B[?25l");
        }
    }
    cursorTo(x, y) {
        if (!__classPrivateFieldGet(this, _Terminal_stream, "f").isTTY) {
            return;
        }
        cursorTo(__classPrivateFieldGet(this, _Terminal_rawStream, "f"), x, y);
    }
    cursorRelative(dx, dy) {
        if (!__classPrivateFieldGet(this, _Terminal_stream, "f").isTTY) {
            return;
        }
        __classPrivateFieldSet(this, _Terminal_dy, __classPrivateFieldGet(this, _Terminal_dy, "f") + dy, "f");
        moveCursor(__classPrivateFieldGet(this, _Terminal_rawStream, "f"), dx, dy);
    }
    cursorRelativeReset() {
        if (!__classPrivateFieldGet(this, _Terminal_stream, "f").isTTY) {
            return;
        }
        moveCursor(__classPrivateFieldGet(this, _Terminal_rawStream, "f"), 0, -__classPrivateFieldGet(this, _Terminal_dy, "f"));
        cursorTo(__classPrivateFieldGet(this, _Terminal_rawStream, "f"), 0);
        __classPrivateFieldSet(this, _Terminal_dy, 0, "f");
    }
    clearRight() {
        if (!__classPrivateFieldGet(this, _Terminal_stream, "f").isTTY) {
            return;
        }
        clearLine(__classPrivateFieldGet(this, _Terminal_rawStream, "f"), 1);
    }
    clearLine() {
        if (!__classPrivateFieldGet(this, _Terminal_stream, "f").isTTY) {
            return;
        }
        clearLine(__classPrivateFieldGet(this, _Terminal_rawStream, "f"), 0);
    }
    clearBottom() {
        if (!__classPrivateFieldGet(this, _Terminal_stream, "f").isTTY) {
            return;
        }
        clearScreenDown(__classPrivateFieldGet(this, _Terminal_rawStream, "f"));
    }
    newline() {
        var _a;
        __classPrivateFieldGet(this, _Terminal_originalWrite, "f").call(this, "\n");
        __classPrivateFieldSet(this, _Terminal_dy, (_a = __classPrivateFieldGet(this, _Terminal_dy, "f"), _a++, _a), "f");
        __classPrivateFieldSet(this, _Terminal_lineActive, false, "f");
    }
    write(s, rawWrite = false) {
        if (__classPrivateFieldGet(this, _Terminal_wrapLines, "f") && !rawWrite) {
            __classPrivateFieldGet(this, _Terminal_originalWrite, "f").call(this, s.slice(0, this.getWidth()));
        }
        else {
            __classPrivateFieldGet(this, _Terminal_originalWrite, "f").call(this, s);
        }
        if (s.length > 0) {
            __classPrivateFieldSet(this, _Terminal_lineActive, true, "f");
        }
    }
    lineWrapping(enabled) {
        if (!this.isTTY()) {
            return;
        }
        __classPrivateFieldSet(this, _Terminal_wrapLines, enabled, "f");
        if (enabled) {
            __classPrivateFieldGet(this, _Terminal_originalWrite, "f").call(this, "\x1B[?7h");
        }
        else {
            __classPrivateFieldGet(this, _Terminal_originalWrite, "f").call(this, "\x1B[?7l");
        }
    }
    isTTY() {
        return __classPrivateFieldGet(this, _Terminal_stream, "f").isTTY;
    }
    getWidth() {
        return __classPrivateFieldGet(this, _Terminal_stream, "f").columns || (__classPrivateFieldGet(this, _Terminal_stream, "f").isTTY ? 80 : 200);
    }
}
_Terminal_stream = new WeakMap(), _Terminal_originalWrite = new WeakMap(), _Terminal_rawStream = new WeakMap(), _Terminal_wrapLines = new WeakMap(), _Terminal_dy = new WeakMap(), _Terminal_lineActive = new WeakMap(), _Terminal_instances = new WeakSet(), _Terminal_interceptForeignWrites = function _Terminal_interceptForeignWrites() {
    const existing = patchedStreams.get(__classPrivateFieldGet(this, _Terminal_stream, "f"));
    if (existing) {
        existing.owners.add(this);
        return;
    }
    const originalWrite = __classPrivateFieldGet(this, _Terminal_originalWrite, "f");
    const owners = new Set([this]);
    patchedStreams.set(__classPrivateFieldGet(this, _Terminal_stream, "f"), { originalWrite, owners });
    __classPrivateFieldGet(this, _Terminal_stream, "f").write = ((...args) => {
        if ([...owners].some((owner) => __classPrivateFieldGet(owner, _Terminal_lineActive, "f"))) {
            originalWrite("\n");
            for (const owner of owners) {
                __classPrivateFieldSet(owner, _Terminal_lineActive, false, "f");
            }
        }
        return originalWrite(...args);
    });
};
