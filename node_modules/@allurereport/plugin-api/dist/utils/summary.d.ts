import { AllureCheckResult, type AllureHistory, type CiDescriptor, type TestResult } from "@allurereport/core-api";
import type { PluginSummary, SummaryCheckResult, TestResultRegistry, TestResultSummary } from "../plugin.js";
import type { AllureStore } from "../store.js";
export declare const convertToTestResultSummary: (tr: TestResult) => TestResultSummary;
export declare const createTestResultRegistry: (testResults: TestResult[]) => TestResultRegistry;
export declare const convertToSummaryCheckResult: (check: AllureCheckResult) => SummaryCheckResult;
export declare const createPluginSummary: (params: {
    filter?: (testResult: TestResult) => boolean;
    name: string;
    plugin: string;
    store: AllureStore;
    history?: AllureHistory;
    ci?: CiDescriptor;
    meta: Record<string, any>;
}) => Promise<PluginSummary>;
