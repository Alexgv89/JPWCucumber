export type ProgressLoggerOptions = {
    total: number;
    message: string;
    unitLabel: string;
    prefix?: string;
    maxLogs?: number;
    debounceMs?: number;
    silent?: boolean;
    log?: (message: string) => void;
};
export type ProgressLogger = {
    log: (force?: boolean) => void;
    increment: (delta?: number) => void;
    getCurrent: () => number;
    cancel?: () => void;
};
export declare const createProgressLogger: ({ total, message, unitLabel, prefix, debounceMs, silent, log, }: ProgressLoggerOptions) => ProgressLogger;
