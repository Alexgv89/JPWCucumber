var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var __classPrivateFieldSet = (this && this.__classPrivateFieldSet) || function (receiver, state, value, kind, f) {
    if (kind === "m") throw new TypeError("Private method is not writable");
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a setter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
    return (kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value)), value;
};
var _UploadQueue_pending, _UploadQueue_suspended;
import { ErrorKind, classifyError, shouldRetryUpload, withUploadRetry } from "./errors.js";
const isSuspendingError = (error) => classifyError(error) === ErrorKind.ResourceRecoverable;
export class UploadQueue {
    constructor() {
        _UploadQueue_pending.set(this, new Map());
        _UploadQueue_suspended.set(this, false);
    }
    get pendingNames() {
        return [...__classPrivateFieldGet(this, _UploadQueue_pending, "f").keys()];
    }
    async run(name, task, options = {}) {
        const { onDeferred, ...retryOptions } = options;
        if (__classPrivateFieldGet(this, _UploadQueue_suspended, "f")) {
            __classPrivateFieldGet(this, _UploadQueue_pending, "f").set(name, task);
            onDeferred?.();
            return { deferred: true };
        }
        try {
            const value = await withUploadRetry(task, retryOptions);
            __classPrivateFieldGet(this, _UploadQueue_pending, "f").delete(name);
            return { deferred: false, value };
        }
        catch (error) {
            if (!shouldRetryUpload(error)) {
                throw error;
            }
            __classPrivateFieldGet(this, _UploadQueue_pending, "f").set(name, task);
            __classPrivateFieldSet(this, _UploadQueue_suspended, isSuspendingError(error), "f");
            onDeferred?.(error);
            return { deferred: true };
        }
    }
    async flush(retryOptions = {}) {
        const remaining = new Map(__classPrivateFieldGet(this, _UploadQueue_pending, "f"));
        for (const [name, task] of __classPrivateFieldGet(this, _UploadQueue_pending, "f")) {
            try {
                await withUploadRetry(task, retryOptions);
                remaining.delete(name);
            }
            catch {
                continue;
            }
        }
        __classPrivateFieldSet(this, _UploadQueue_pending, remaining, "f");
        __classPrivateFieldSet(this, _UploadQueue_suspended, remaining.size > 0, "f");
        return [...remaining.keys()];
    }
}
_UploadQueue_pending = new WeakMap(), _UploadQueue_suspended = new WeakMap();
