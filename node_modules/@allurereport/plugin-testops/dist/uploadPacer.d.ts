import type { UploadRateLimit } from "./model.js";
export type UploadCost = {
    requests?: number;
    files?: number;
    bytes?: number;
};
export declare const DEFAULT_UPLOAD_RATE_LIMIT: UploadRateLimit;
export declare class UploadPacer {
    #private;
    constructor(rateLimit: UploadRateLimit | false | undefined, now?: () => number);
    wait(cost: UploadCost, signal?: AbortSignal): Promise<void>;
}
