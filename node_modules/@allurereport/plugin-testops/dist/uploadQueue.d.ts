import { type RetryOptions } from "./errors.js";
export type UploadRunResult<T> = {
    deferred: false;
    value: T;
} | {
    deferred: true;
};
export declare class UploadQueue {
    #private;
    get pendingNames(): string[];
    run<T>(name: string, task: () => Promise<T>, options?: RetryOptions & {
        onDeferred?: (error?: unknown) => void;
    }): Promise<UploadRunResult<T>>;
    flush(retryOptions?: RetryOptions): Promise<string[]>;
}
