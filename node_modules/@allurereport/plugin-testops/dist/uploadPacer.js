var __classPrivateFieldSet = (this && this.__classPrivateFieldSet) || function (receiver, state, value, kind, f) {
    if (kind === "m") throw new TypeError("Private method is not writable");
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a setter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
    return (kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value)), value;
};
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _UploadPacer_instances, _UploadPacer_requests, _UploadPacer_files, _UploadPacer_bytes, _UploadPacer_now, _UploadPacer_reserve, _UploadPacer_reserveAt;
import { setTimeout as delay } from "node:timers/promises";
export const DEFAULT_UPLOAD_RATE_LIMIT = {
    windowMs: 1000,
    maxRequestsPerWindow: 20,
    maxFilesPerWindow: 1000,
    maxBytesPerWindow: 1024 * 1024 * 1024,
};
const isBudgetEnabled = (budget) => budget.windowMs > 0 && budget.limit > 0;
const scaledWindowCost = (windowMs, cost, limit) => {
    if (windowMs <= 0 || cost <= 0 || limit <= 0) {
        return 0;
    }
    return (windowMs * cost) / limit;
};
const resolveRateLimit = (rateLimit) => {
    const rateLimitUnset = rateLimit === undefined;
    const rateLimitDisabled = rateLimit === false;
    if (rateLimitUnset) {
        return DEFAULT_UPLOAD_RATE_LIMIT;
    }
    if (rateLimitDisabled) {
        return undefined;
    }
    return rateLimit;
};
export class UploadPacer {
    constructor(rateLimit, now = Date.now) {
        _UploadPacer_instances.add(this);
        _UploadPacer_requests.set(this, void 0);
        _UploadPacer_files.set(this, void 0);
        _UploadPacer_bytes.set(this, void 0);
        _UploadPacer_now.set(this, void 0);
        const resolvedRateLimit = resolveRateLimit(rateLimit);
        const windowMs = resolvedRateLimit?.windowMs ?? 0;
        __classPrivateFieldSet(this, _UploadPacer_requests, { limit: resolvedRateLimit?.maxRequestsPerWindow ?? 0, windowMs, next: 0 }, "f");
        __classPrivateFieldSet(this, _UploadPacer_files, { limit: resolvedRateLimit?.maxFilesPerWindow ?? 0, windowMs, next: 0 }, "f");
        __classPrivateFieldSet(this, _UploadPacer_bytes, { limit: resolvedRateLimit?.maxBytesPerWindow ?? 0, windowMs, next: 0 }, "f");
        __classPrivateFieldSet(this, _UploadPacer_now, now, "f");
    }
    async wait(cost, signal) {
        const waitMs = __classPrivateFieldGet(this, _UploadPacer_instances, "m", _UploadPacer_reserve).call(this, __classPrivateFieldGet(this, _UploadPacer_now, "f").call(this), cost);
        if (waitMs <= 0) {
            return;
        }
        await delay(waitMs, undefined, signal ? { signal } : undefined);
    }
}
_UploadPacer_requests = new WeakMap(), _UploadPacer_files = new WeakMap(), _UploadPacer_bytes = new WeakMap(), _UploadPacer_now = new WeakMap(), _UploadPacer_instances = new WeakSet(), _UploadPacer_reserve = function _UploadPacer_reserve(now, cost) {
    let start = now;
    for (const budget of [__classPrivateFieldGet(this, _UploadPacer_requests, "f"), __classPrivateFieldGet(this, _UploadPacer_files, "f"), __classPrivateFieldGet(this, _UploadPacer_bytes, "f")]) {
        if (isBudgetEnabled(budget) && budget.next > start) {
            start = budget.next;
        }
    }
    __classPrivateFieldGet(this, _UploadPacer_instances, "m", _UploadPacer_reserveAt).call(this, __classPrivateFieldGet(this, _UploadPacer_requests, "f"), start, cost.requests ?? 0);
    __classPrivateFieldGet(this, _UploadPacer_instances, "m", _UploadPacer_reserveAt).call(this, __classPrivateFieldGet(this, _UploadPacer_files, "f"), start, cost.files ?? 0);
    __classPrivateFieldGet(this, _UploadPacer_instances, "m", _UploadPacer_reserveAt).call(this, __classPrivateFieldGet(this, _UploadPacer_bytes, "f"), start, cost.bytes ?? 0);
    return start > now ? start - now : 0;
}, _UploadPacer_reserveAt = function _UploadPacer_reserveAt(budget, start, cost) {
    if (!isBudgetEnabled(budget) || cost <= 0) {
        return;
    }
    budget.next = start + scaledWindowCost(budget.windowMs, cost, budget.limit);
};
