import { type MetricSample, type TestError, type TestResult } from "@allurereport/core-api";
import type { QualityGateConfig, QualityGateMetricHistoryPoint, QualityGateValidationResult } from "@allurereport/plugin-api";
export declare const stringifyQualityGateResults: (results: QualityGateValidationResult[]) => string;
export declare const convertQualityGateResultsToTestErrors: (results: QualityGateValidationResult[]) => TestError[];
export declare class QualityGateState {
    #private;
    setResult(rule: string, value: unknown, testResults?: string[]): void;
    getResult(rule: string): unknown;
    getTestResults(rule: string): string[];
    markTestResultsProcessed(testResults: TestResult[]): void;
    isTestResultProcessed(trId: string): boolean;
}
export declare class QualityGate {
    private readonly config;
    constructor(config: QualityGateConfig);
    validate(payload: {
        state?: QualityGateState;
        trs: TestResult[];
        metrics?: MetricSample[];
        previousHistory?: QualityGateMetricHistoryPoint[];
        environment?: string;
    }): Promise<{
        fastFailed: boolean;
        results: QualityGateValidationResult[];
    }>;
}
